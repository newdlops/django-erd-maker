# UI Graph Mock Fixtures

This directory stores mock graph payloads for renderer and state tests.

Mock payloads are acceptable for lower-level UI verification only. They must not be counted as end-to-end coverage.
