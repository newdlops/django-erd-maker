# Analyzer JSON Fixtures

This directory stores stable JSON payloads for contract and graph-level tests.

These fixtures are useful for focused lower-level tests, but they do not replace real-workspace E2E coverage.
